set working-directory := "../.."

[private]
default:
    just --list --justfile {{source_file()}}

# Lint Markdown files
markdown:
    rumdl check .

# Run every linter
all: markdown

# --- Add once the Gradle project lands, e.g.: ---
# java:
#     ./gradlew spotlessCheck
