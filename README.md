# jtbrl
Token Bucket Rate Limiter 

## Idea

This is a standard practice exercise - to implement a token-bucket rate limiter.

So I figured out that I could as well try and do it.

### Requirements

The core requirement is that a client cannot make more than X requests 
per second/minute/hour with their tokens